# V10：二态条件化约束复盘

## 官网反馈

- V9：`327,052` Token，官网 `86.2732`，对应 `88/100`。
- V10：`327,052` Token，官网 `85.2928`，对应 `87/100`。
- V10 只把 `fc_a_012` 从 ACD 恢复为 AC、把 `fc_a_015` 从 B 恢复为 A。
- 两处变化合计净减 1，因此其中一处 V9 答案命中，另一处的新旧答案都未命中。

## 被推翻的假设

V10 之前假设每道变化题的真实标签只能是旧答案或新答案。若该假设成立，偶数道
变化的净分差必须为偶数；但 V10 相对 V7 修改 4 题只净增 1。官方结果因此直接
证明存在第三标签状态，先前的“四增二减”“推断 90/100”和条件化 28 道必对题
结论全部作废。

保留 V10 配置和提交文件仅为可复现实验，不再作为候选基线：

- `outputs/v10_inferred90/answer.csv`
- SHA-256：`4DD820F71F868866E224A79CC5901E9165EF2919BF1CB04D83065D3D7D92A8B0`

## 仍然有效的结论

- 扩大到 30,000 字上下文的全量深证据实验使用 `783,244` Token 并产生 25 个
  反转，不能作为自动覆盖规则。
- V10 反馈加入全部可信官网运行后，V9 的 `reg_a_004=AC` 被约束证明必错。
- `fc_a_004=A`、`fc_a_015=A`、`reg_a_001=CD`、`res_a_011=BC` 等候选与
  历史官网正确题数矛盾，不能提交。

后续三态模型与 V11 候选见
[V11_TERNARY_CONSTRAINTS.md](V11_TERNARY_CONSTRAINTS.md)。
